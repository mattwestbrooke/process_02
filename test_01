##### pass

import output_object_classes

#output_objects = output_objects_classes.Ouput_Object_List()

import os
import sys

ROOT_DIR = os.path.dirname(sys.modules['__main__'].__file__)

def get_all_current_trees():
    trees = []
    trees_path = ROOT_DIR + '/trees'
    for file in os.listdir(trees_path):
        if file.endswith(".tree"):
            file_split = file.split('.')
            trees.append(file_split[0])

    return trees



trees = get_all_current_trees()
for i in trees:
    print i



